makefile instructions:

make run: runs the game as usual

make test: compiles the testing classes

make testGB: tests the gameboard class using a predefined testing suite

make testGBmem: tests the gameboardMem class using a predefined testing suite

make clean: force removes all .class files